// import * as cdk from 'aws-cdk-lib';
// import { Template } from 'aws-cdk-lib/assertions';
// import * as SimpleCodeScanningPipeline from '../lib/scsp-pipeline-stack';

// example test. To run these tests, uncomment this file along with the
// example resource in lib/scsp-pipeline-stack.ts
test('Pipeline Created', () => {
    //   const app = new cdk.App();
    //     // WHEN
    //   const stack = new SimpleCodeScanningPipeline.SimpleCodeScanningPipelineStack(app, 'MyTestStack');
    //     // THEN
    //   const template = Template.fromStack(stack);
    //   template.hasResourceProperties("AWS::CodePipeline::Pipeline");
});
